"use strict";

$(function() {
    $.jqluster.init("display", "[% transport_id | js %]");

    var showQRCode = function() {
        $("#feeditem-display").hide();
        $("#qrcode").show();
    };
    showQRCode();
    $(".qr-code-on").on("click", showQRCode);
    
    $.jqluster("headlines", function($_) {
        $_("#feeditem-list").off("click", "li");
        $_("#feeditem-list").on("click", "li", function() {
            $_(this).find(".feeditem-id").text().then(function(item_id) {
                return $.ajax({
                    type: "GET",
                    url: "body.html",
                    data: {id: item_id},
                    dataType: "html",
                });
            }).then(function(data, text_status, jq_xhr) {
                $("#qrcode").hide();
                $("#feeditem-display").html(data).show();
            }).then(null, function(error) {
                console.error("Load feeditem error: ");
                console.error(error);
            });
        });
        
        $_(".headlines-finish").off("click");
        $_(".headlines-finish").on("click", showQRCode);
    });
});
